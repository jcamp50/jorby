# Public browser configuration only. Never put Foundry service secrets,
# FOUNDRY_TOKEN, provider keys, or invite hashes here.
#
# FOUNDRY_TOKEN is a shell/CI secret used only to download @jorby/sdk.
# It is not a VITE_ variable and must never enter the browser bundle.

VITE_FOUNDRY_API_URL=https://jordca.usw-22.palantirfoundry.com
VITE_FOUNDRY_CLIENT_ID=
VITE_FOUNDRY_REDIRECT_URL=http://localhost:8080/auth/callback
VITE_MEMBERSHIP_SERVICE_URL=
