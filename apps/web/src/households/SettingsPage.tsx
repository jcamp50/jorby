import { SectionPlaceholder } from '@/app/shell/SectionPlaceholder'

export function SettingsPage() {
  return (
    <SectionPlaceholder
      title="Household settings"
      description="Invites, members, and profile live here after the membership service exists. Any member may invite or remove."
    />
  )
}
