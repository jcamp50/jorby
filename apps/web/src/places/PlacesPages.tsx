import { SectionPlaceholder } from '@/app/shell/SectionPlaceholder'

export function PlacesIndexPage() {
  return (
    <SectionPlaceholder
      title="Places"
      description="Search-to-add from Foundry provider Functions after mock adapters exist. Do not call Google Places from the browser."
    />
  )
}

export function PlaceDetailPage() {
  return (
    <SectionPlaceholder
      title="Place"
      description="Visit state, visits, dishes, and per-member reviews."
    />
  )
}
